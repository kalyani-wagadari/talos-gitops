apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: echo-data
  namespace: kalyani
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: hcloud-volumes-encrypted
